public class DeParser {
    // takes a response object and converts it to a string
    // implemented as a decorator between the response generator and the client socket

    public boolean exists() { return true; }
}
